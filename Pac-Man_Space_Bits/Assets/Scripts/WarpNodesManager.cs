using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WarpNodesManager : MonoBehaviour
{

    [Header("Warp Nodes")]
    public Node WarpNodeLeft;
    public Node WarpNodeRight;


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
